// we need a paginator, probably based on <input type="radio" name="pageSize" /> 
// that sets the value of pageSize to 0, the total, next, last or `undefined` depending
// and then is styled up to look like the other Paginator

export default () => 'Static Paginator'